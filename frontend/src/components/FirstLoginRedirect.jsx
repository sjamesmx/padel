import { useNavigate } from 'react-router-dom';

const FirstLoginRedirect = () => {
  const navigate = useNavigate();
  // Usa navigate si es necesario, por ejemplo:
  // navigate('/onboarding');
  return null; // O tu JSX
};

export default FirstLoginRedirect;